input_dir: /lustre/fsn1/projects/rech/jsy/uzj81mi/demo_data
input_pattern: "*.tif"

# Folder of keras reference label TIFFs. Must contain a file with the
# same basename as the input timelapse — that's the comparison ground
# truth (or the keras model's prediction we're benchmarking against).
keras_dir: /lustre/fsn1/projects/rech/jsy/uzj81mi/demo_data/keras_prediction

# CSV lands here as ``<input_stem>.compare.csv``.
out_dir: /lustre/fsn1/projects/rech/jsy/uzj81mi/demo_data/compare
