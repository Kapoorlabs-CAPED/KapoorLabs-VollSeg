base_data_dir: "/lustre/fsn1/projects/rech/jsy/uzj81mi/segmentation_prediction/"
input_dir: raw
output_dir: combo

# Per-role model location. Leave both fields of a role empty/null to
# disable that role for the run. Either disk path (log_path) or a HF
# repo (hf_repo_id) works — same contract as the singleton scripts.
stardist:
  log_path: "/lustre/fsn1/projects/rech/jsy/uzj81mi/models_stardist_pytorch/"
  hf_repo_id: "KapoorLabs/xenopus-stardist-pytorch"
  hf_model_dir: ""

unet:
  log_path: "/lustre/fsn1/projects/rech/jsy/uzj81mi/models_unet_pytorch/"
  hf_repo_id: "KapoorLabs/xenopus-unet-pytorch"
  hf_model_dir: ""

maskunet:
  log_path: "/lustre/fsn1/projects/rech/jsy/uzj81mi/models_maskunet_pytorch/"
  hf_repo_id: "KapoorLabs/xenopus-maskunet-pytorch"
  hf_model_dir: ""
